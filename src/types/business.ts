export interface BusinessOwnership{ personId:string; share:number; }
export interface LogisticsBusiness{ id:string; name:string; foundedDate:string; ownerShares:BusinessOwnership[]; cash:number; debt:number; reputation:number; valuation:number; status:'active'|'closed'|'bankrupt'; facilityIds:string[]; employeeIds:string[]; contractIds:string[]; pricingIndex:number; staffingTarget:number; }
export interface WarehouseFacility{ id:string; businessId:string; location:string; squareFeet:number; palletCapacity:number; monthlyRent:number; condition:number; automationLevel:number; utilization:number; }
export interface BusinessEmployee{ id:string; businessId:string; personId:string|null; name:string; role:'associate'|'lead'|'supervisor'|'manager'|'sales'|'admin'; annualPay:number; productivity:number; reliability:number; morale:number; safety:number; active:boolean; }
export interface LogisticsContract{ id:string; businessId:string; customerName:string; monthlyUnits:number; revenuePerUnit:number; serviceTarget:number; complexity:number; startDate:string; endDate:string; status:'offered'|'active'|'ended'|'lost'; }
export interface LogisticsKpiSnapshot{ id:string; businessId:string; date:string; productivity:number; inventoryAccuracy:number; serviceLevel:number; safety:number; laborCost:number; facilityUtilization:number; revenue:number; operatingProfit:number; }
export interface BusinessOpportunity{ id:string; type:'customer_contract'|'facility'|'acquisition'; title:string; description:string; requiredCash:number; expiresDate:string; accepted:boolean; }
export interface BusinessWorldState{ businesses:LogisticsBusiness[]; warehouseFacilities:WarehouseFacility[]; businessEmployees:BusinessEmployee[]; logisticsContracts:LogisticsContract[]; logisticsKpis:LogisticsKpiSnapshot[]; businessOpportunities:BusinessOpportunity[]; }
